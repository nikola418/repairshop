import { Metadata } from "next";
import { FC } from "react";

export const metadata: Metadata = {
  title: "Customers",
};

const Customers: FC = () => <h2>Customers Page</h2>;

export default Customers;
